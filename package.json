{
  "name": "koora-live",
  "version": "1.0.0",
  "main": "api-server.js",
  "scripts": {
    "start": "node api-server.js",
    "build": "pnpm --filter @workspace/football-scores run build && rm -rf dist && cp -r artifacts/football-scores/dist ./dist"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^2.7.0"
  }
}
