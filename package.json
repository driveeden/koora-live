{
  "name": "koora-live",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.26.1",
  "scripts": {
    "dev:api": "node artifacts/api-server/dist/index.mjs",
    "build:frontend": "BASE_PATH=/ pnpm --filter @workspace/football-scores build",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@vercel/node": "^5.1.0",
    "@types/node": "^22.0.0"
  }
}
