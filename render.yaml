services:
  - type: web
    name: koora-live
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node api-server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
